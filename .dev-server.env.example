# 开发环境服务器配置模板
# 复制为 .dev-server.env 后按实际情况修改

DEV_HOST=192.168.1.223
DEV_USER=root
DEV_PORT=22
DEV_PATH=/opt/PPIO-GPT-image2

# 默认使用当前仓库 origin 的 fetch 地址，可留空
DEV_GIT_URL=

# 如果开发环境服务器只能密码登录，可填写密码
# 留空则走当前 SSH agent / 私钥
DEV_SSH_PASS=

# 1 表示同步本地 .env 到开发环境服务器；0 表示不处理 .env
DEV_SYNC_ENV=1
